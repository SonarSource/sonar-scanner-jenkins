language: java
sudo: false
install: true

jdk:
  - oraclejdk7

script:
  - ./travis.sh
env:
  - JOB=CI
  - JOB=IT-SQDEV
  - JOB=IT-SQLTS

matrix:
  fast_finish: true

cache:
  directories:
    - '$HOME/.m2/repository'

notifications:
  email: false
